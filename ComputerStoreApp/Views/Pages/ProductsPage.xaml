<Page x:Class="ComputerStoreApp.Views.Pages.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Товары"
      Background="{StaticResource BackgroundColor}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="10" Padding="15" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Каталог товаров" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
                <TextBlock Text="Выберите товары для покупки" FontSize="12" Foreground="Gray" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="15" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="txtSearch" Grid.Column="0" TextChanged="TxtSearch_TextChanged" Height="40"/>
                <ComboBox x:Name="cmbCategory" Grid.Column="1" Width="200" SelectionChanged="CmbCategory_SelectionChanged" Height="40" Margin="10,0,0,0"/>
                <ComboBox x:Name="cmbManufacturer" Grid.Column="2" Width="200" SelectionChanged="CmbManufacturer_SelectionChanged" Height="40" Margin="10,0,0,0"/>
                <Button x:Name="btnReset" Grid.Column="3" Content="Сбросить фильтры" Width="140" Height="40" Margin="10,0,0,0" Background="{StaticResource WarningColor}" Click="BtnReset_Click"/>
            </Grid>
        </Border>

        <DataGrid x:Name="dgProducts" Grid.Row="2" AutoGenerateColumns="False" SelectionMode="Single" CanUserAddRows="False" IsReadOnly="True" RowHeight="50">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Артикул" Binding="{Binding SKU}" Width="100"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="250"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="120"/>
                <DataGridTextColumn Header="Производитель" Binding="{Binding Manufacturer}" Width="120"/>
                <DataGridTextColumn Header="Гарантия" Binding="{Binding WarrantyMonths}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="{Binding WarrantyMonths, StringFormat='{}{0} мес.'}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat='{}{0:C}'}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{StaticResource SuccessColor}"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTemplateColumn Header="Действия" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="🛒 В корзину" Click="AddToCart_Click" Tag="{Binding ProductId}" Height="30" Padding="10,5"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" Background="White" CornerRadius="5" Padding="10" VerticalAlignment="Bottom" Margin="0,0,0,10">
            <TextBlock x:Name="txtStatus" Foreground="Gray" FontSize="11"/>
        </Border>
    </Grid>
</Page>